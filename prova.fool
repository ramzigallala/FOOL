let
  var y:int = 5+2;
  fun g:bool (b:bool)
    let
      fun f:bool (n:int, m:int)
        let
          var x:int = m;
        in g(3==y);
    in if b then { f(2,3) } else { false };
in
  print (
    if (10>=y)
      then { 5 }
      else { 10 }
  );